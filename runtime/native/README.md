# Native runtime

This directory will contain Rust implementations behind the versioned Zryna native ABI. Compiler crates may depend on ABI contracts, but runtime implementations must not depend on compiler phases.
