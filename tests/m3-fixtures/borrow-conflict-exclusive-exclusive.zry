function bad(): i32 {
  let root: i32 = 7;
  {
    const first: BorrowMut<i32> = borrowMut(root);
    const second: BorrowMut<i32> = borrowMut(root);
    first = 8;
    second = 9;
  }
  return root;
}
