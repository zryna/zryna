function choose(): bool {
  const root: bool = true;
  if (root) {
    {
      const alias: Borrow<bool> = borrow(root);
      const value: bool = alias;
    }
  } else {
    {
    }
  }
  return root;
}
