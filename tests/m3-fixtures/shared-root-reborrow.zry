function read(): i32 {
  const root: i32 = 7;
  {
    const parent: Borrow<i32> = borrow(root);
    const child: Borrow<i32> = borrow(parent);
    const a: i32 = parent;
    const b: i32 = child;
  }
  return root;
}
