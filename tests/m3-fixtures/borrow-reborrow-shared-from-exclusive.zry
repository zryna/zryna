function bad(): i32 {
  let root: i32 = 7;
  {
    const parent: BorrowMut<i32> = borrowMut(root);
    const child: Borrow<i32> = borrow(parent);
    parent = 8;
    const copy: i32 = child;
  }
  return root;
}
