function relay(loan: Borrow<String>): String { return clone(loan); } function caller(incoming: String): String { let items: String = incoming; { const loan: Borrow<String> = borrow(items); const seen: String = relay(loan); } return items; }
