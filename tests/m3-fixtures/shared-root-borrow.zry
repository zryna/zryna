function read(): i32 {
  const root: i32 = 7;
  {
    const first: Borrow<i32> = borrow(root);
    const a: i32 = first;
    const second: Borrow<i32> = borrow(root);
    const b: i32 = second;
  }
  return root;
}
