function values(index: i32): i32 {
  const items: Vec<i32> = Vec<i32>([7, 9]);
  const result: i32 = items[index];
  const later: String = "later";
  return result;
}

export function score(index: i32): i32 { return values(index); }
