function values(): i32 {
  const items: Vec<i32> = Vec<i32>([7, 9]);
  const moved: Vec<i32> = items;
  const copied: Vec<i32> = clone(items);
  return copied[1];
}

export function score(): i32 { return values(); }
